spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/srinudb
    username: srinu
    password: srinu
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update